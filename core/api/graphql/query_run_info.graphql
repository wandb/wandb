query QueryRunInfo(
    $entity: String!,
    $project: String!,
    $run: String!,
) {
    project(name: $project, entityName: $entity) {
        run(name: $run) {
            displayName
            summaryMetrics
            events
        }
    }
}
