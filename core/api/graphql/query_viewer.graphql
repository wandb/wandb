# @genqlient(omitempty: true)
query Viewer{
    viewer {
        id
        entity
        username
        email
        flags
        teams {
            edges {
                node {
                    name
                }
            }
        }
    }
}
