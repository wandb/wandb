query QueryRunHistoryKeys(
    $entity: String!,
    $project: String!,
    $run: String!,
) {
    project(name: $project, entityName: $entity) {
        run(name: $run) {
            historyKeys
        }
    }
}
