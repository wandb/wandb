mutation UpsertSweepState(
    $id: ID!,
    $state: String!,
) {
    upsertSweep(
        input: {
            id: $id,
            state: $state,
        }
    ) {
        sweep {
            state
        }
    }
}
