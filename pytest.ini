[pytest]
markers =
    image_output
    multiclass
    wandb_args
testpaths = tests
timeout = 60
filterwarnings =
    ignore::DeprecationWarning

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
