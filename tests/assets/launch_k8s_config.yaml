apiVersion: v1

clusters:
  - name: test-cluster
    cluster: {}

contexts:
  - context:
      cluster: test-cluster
      user: test-user
    name: active-context

current-context: active-context

users:
  - name: test-user
    user: {}
