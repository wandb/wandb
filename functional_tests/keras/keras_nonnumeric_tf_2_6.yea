id: 0.keras.nonnumericlayers
tag:
    shard: tf26
plugin:
    - wandb
command:
    program: test_keras_nonnumeric.py
depend:
    requirements:
        - tensorflow==2.6.0
        - pandas
assert:
    - :wandb:runs_len: 1
    - :op:contains_regex:
        - :wandb:runs[0][output][stderr]
        - .*Non-numeric values found in layer.*parameters/lookupembark_town.*1, not logging this layer.*
    - :wandb:runs[0][exitcode]: 0
