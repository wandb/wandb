fragment TagFragment on Tag {
  __typename
  id
  name # The actual string name of the tag
}

fragment ArtifactAliasFragment on ArtifactAlias {
  __typename
  id
  alias # The actual string name of the alias
}

fragment PageInfoFragment on PageInfo {
  __typename
  endCursor
  hasNextPage
}

fragment OrgInfoFragment on Organization {
  name
  orgEntity {
    name
  }
}

query FetchOrgInfoFromEntity($entity: String!) {
  entity(name: $entity) {
    organization {
      ...OrgInfoFragment
    }
    user {
      organizations {
        ...OrgInfoFragment
      }
    }
  }
}

query FetchOrganization($org: String!) {
  organization(name: $org) {
    id
    name
    orgEntity {
      __typename
      id
      name
      entityType
    }
  }
}

# A generic probe of the feature flags evaluated for an organization.
# Note: `OrgName` here is deliberate, and `OrgId` is NOT equivalent.
# Org-targeted feature gates are included regardless of the ramp ID type, but
# only `OrgName` also selects legacy backend ramps keyed by org name, which
# some probes still need on older servers.
query OrgFeatureFlags($org: String!) {
  organization(name: $org) {
    featureFlags(rampIDType: OrgName) {
      rampKey
      isEnabled
    }
  }
}
