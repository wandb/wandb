query ProbeFields($type: String!) {
  typeInfo: __type(name: $type) {
    fields {
      name
    }
  }
}

query ProbeInputFields($type: String!) {
  typeInfo: __type(name: $type) {
    inputFields {
      name
    }
  }
}
