name: Test Comment

on:
  issue_comment:
    types: [created, edited]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test body Comment
        run: echo ${{ github.event.comment.body }}
